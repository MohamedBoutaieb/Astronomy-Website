{% extends 'base.html.twig' %}

{% block body %}
<div class="container">


    <table class="table table-hover">
        <thead>
        <tr style="color: whitesmoke">
            <th scope="col">username</th>
            <th scope="col">first name</th>
            <th scope="col">last name</th>

        </tr>
        </thead>
        {% for user in users %}
            <tr class="table-light">

                <td>{{ user.username }}</td>
                <td>{{ user.firstName}}</td>
                <td>{{ user.lastname}}</td>

                <td> <a href={{ path('details' , {id :user.username}) }}>  <button class="btn-info" > more details</button></a> </td>
            </tr>
        {% endfor %}

    </table>
</div>
{% endblock %}

{% block title %}

{% endblock %}

