{% extends 'account_details/user.html.twig' %}

{% block title %} Edit Address{% endblock %}

{% block main %}
    <div class="container">
        <h1 style="color: white">Profile Settings</h1>
{#        {{ form(form) }}#}

        {{ form_start(form) }}
        {% for att in form %}
            {% if att.vars.label|title!= "token" %}
                {{ att.vars.label }}
                {{ form_label(att, null, {'label_attr': {'style': 'color:white'}}) }}
                {{ form_widget(att) }}
            {% endif %}
        {% endfor %}

        {{ form_end(form) }}
    </div>

{% endblock %}
